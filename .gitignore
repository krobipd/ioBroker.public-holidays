# macOS
.DS_Store

# Node.js
node_modules/
npm-debug.log*

# IDE
.idea/
.vscode/*
!.vscode/settings.json
*.swp
*.swo

# ioBroker repochecker
.commitinfo

# ioBroker
admin/i18n/flat.txt

# Build & Test
coverage/
.nyc_output/
*.tsbuildinfo

# dev-server (local ioBroker test instance)
.dev-server/

# Environment
.env
.env.local

# Admin custom component: the BUILT admin/custom/ stays tracked so the component ships with the
# npm package exactly as it was verified, without a build step in between. (The original reason —
# carrying it into a GitHub-URL install — is gone: `common.nogit` switched that install path off,
# and the npm deploy has been unblocked since 2026-08-21.) Only the src-admin work dirs are ignored.
/src-admin/build
/src-admin/.__mf__temp
/src-admin/.mf
/src-admin/node_modules
/src-admin/package-lock.json

# Claude Code local files (not published)
.claude/settings.local.json
.claude/dev-history.md
CLAUDE.local.md

# Build output is not versioned: the CI builds before test and publish (common.nogit)
build/

# Vite emits a declaration next to every src/ module the admin card imports — measured 2026-09-06:
# `vite build` inside src-admin/ does it, `tsc` does not. Generated output, never committed; type
# resolution always prefers the .ts, so a stale one cannot shadow the source (also measured).
/src/**/*.d.ts
!/src/lib/adapter-config.d.ts
