# macOS
.DS_Store

# Node.js
node_modules/
npm-debug.log*

# IDE
.idea/
.vscode/*
!.vscode/settings.json
*.swp
*.swo

# ioBroker repochecker
.commitinfo

# ioBroker
admin/i18n/flat.txt

# Build & Test
coverage/
.nyc_output/
*.tsbuildinfo

# dev-server (local ioBroker test instance)
.dev-server/

# Environment
.env
.env.local

# Admin custom component: the BUILT admin/custom/ is tracked (like build/) so a GitHub-URL install
# carries the component — needed for this admin-8-only release, whose npm deploy stays blocked by
# the version-gate until admin 8 is stable. Only the src-admin work dirs stay ignored.
/src-admin/build
/src-admin/.__mf__temp
/src-admin/node_modules
/src-admin/package-lock.json
.dev-server/

# Claude Code local files (not published)
.claude/settings.local.json
.claude/dev-history.md
CLAUDE.local.md
